package edu.psu.ist.controller;

import edu.psu.ist.view.ItemView;

import java.awt.event.ActionListener;

public class ItemController implements ActionListener {
    private ItemView itemView;

    public ItemController() {
        ItemView itemView1 = new ItemView(this);
        //get button
    }
    /*
    //Instance Variables
    CurrentPackingList theInstrumentList;
    InstrumentListUI theInstrumentListUI;

    public ItemView() {

        //Get the instrument list
        //When instantiated it will create a list of individual Instruments
        CurrentPackingList = new CurrentPackingList();

        //Show the list of model objects in the UI
        theInstrumentUI = new InstrumentUI(this);
        theInstrumentListUI.setVisible(true);

     */
}
